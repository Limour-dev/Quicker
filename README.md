# Quicker
```conda
conda install conda-forge::mamba
mamba create -n Quicker conda-forge::pyperclip conda-forge::keyboard conda-forge::sortedcontainers
# conda install conda-forge::pyperclip conda-forge::keyboard conda-forge::sortedcontainers
```
+ `win+r` 将 `RunPy.bat` 的快捷方式放入 `shell:startup` 文件夹
+ 测试时手动运行 `RunAsAdmin.bat`
