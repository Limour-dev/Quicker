# Quicker
```conda
mamba create -n Quicker conda-forge::pyperclip conda-forge::keyboard
# conda install conda-forge::pyperclip conda-forge::keyboard
```
+ 计划任务，管理员权限运行 `RunPy.bat`
+ 测试时手动运行 `RunAsAdmin.bat`
